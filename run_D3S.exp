#!/usr/bin/expect -f

set interval [lindex $argv 0];

spawn sudo python /home/pi/dosenet-raspberrypi-1/D3S_rabbitmq_DAQ.py -i $interval
#spawn sudo touch /home/pi/test.py

expect "password for pi:"
send -- "piistasty!\r"

#interact

expect eof
exit
